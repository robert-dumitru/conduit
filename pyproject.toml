[project]
name = "conduit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.11.7",
]

[dependency-groups]
dev = [
    "ruff>=0.12.11",
]
